cbor==1.0.0
coppeliasim_zmqremoteapi_client==2.0.4
et_xmlfile==2.0.0
numpy==2.3.1
opencv-python==4.13.0.92
openpyxl==3.1.5
paho-mqtt==2.1.0
pandas==3.0.2
python-dateutil==2.9.0.post0
pyzmq==27.0.0
six==1.17.0
# --- esp32_mock.py ---
# `esp32_mock.py` already falls back to TensorFlow when `tflite-runtime`
# is unavailable. Python 3.13 currently lacks `tflite-runtime` wheels in
# many environments, so keep it optional there to avoid breaking the base
# controller installation.
tflite-runtime>=2.14; python_version < "3.13"
python-dotenv>=1.0
